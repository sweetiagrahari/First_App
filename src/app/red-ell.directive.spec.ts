import { RedEllDirective } from './red-ell.directive';

describe('RedEllDirective', () => {
  it('should create an instance', () => {
    const directive = new RedEllDirective();
    expect(directive).toBeTruthy();
  });
});
